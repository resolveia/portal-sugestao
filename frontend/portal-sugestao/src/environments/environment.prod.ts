export const environment = {
  production: true,
  apiUrl: '/api',
  // TODO: preencher com a URL real da api_authentication de produção quando o time do ERP definir
  // (ainda não existe — ver docs/autenticacao-e-api-portal-sugestoes.md).
  authApiUrl: '/api-authentication'
};
